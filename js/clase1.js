// js uses camel case
// var is not recomended

var nombrePersona = "Andres";

// homework: difference between let and var

let nombre = "Juan";

const PERSONA = "Roberto";

// ; is used for good practice

console.log(nombrePersona)

// to run js files in console: node {path to file}

// strings formateados
// we use backticks

console.log(`${nombrePersona} ${nombre}`)

let nombreIngresado = prompt("Por favor ingrese su nombre")
let apellidoIngresado = prompt("Por favor ingrese su apellido")

console.log(`${nombreIngresado} ${apellidoIngresado}`)
